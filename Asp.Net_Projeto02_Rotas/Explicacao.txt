1 - Arquivo: Views/Home/Produtos.cshtml (e Contato.cshtml)

Explicação: São os documentos de interface. Aqui escrevemos apenas o conteúdo necessário da página. Todo o resto do HTML (como tags <html>, <head>, scripts e estilos) é montado automaticamente pelo arquivo _Layout.cshtml.

@{ 
    ViewData["Title"] = "Nossos Produtos";     // Valor enviado para a tag <title> no arquivo de Layout
} 

<h1>Página de Produtos</h1>
<p>Aqui você verá a lista de itens da loja.</p>
<a href="/">Voltar para Home</a>


2 -  Arquivo: Controllers/HomeController.cshtml

Explicação: Responsável pela lógica das rotas. Ao dar o mesmo nome para a função, o .NET já procura automaticamente o arquivo correspondente dentro da pasta Views e da subpasta Home.

public IActionResult Produtos()     // Apenas escrever o nome do arquivo cshtml que ele já encontra na pasta Views/Home
    {
    return View();
    }


3 - Arquivo: Program.cs (Trecho final)

Explicação: É o configurador da Rota Raiz. Ele define qual é o primeiro documento que o servidor deve buscar "por debaixo dos panos" quando o endereço do site é acessado sem caminhos adicionais.


// Define o "Documento de Abertura" do site
app.MapControllerRoute(                        
    name: "default",                           // Nome da regra de roteamento padrão
    pattern: "{controller=Home}/{action=Index}/{id?}") // Se a URL vier vazia, preenche com Home/Index automaticamente
    .WithStaticAssets();                       // Gerencia automaticamente o carregamento de CSS, imagens e JS





*** Comandos de Attribute Routing e Razor Syntax *** 


--> Categoria: Lógica e Rotas (Controller)

Explicação: O uso do [Route] permite personalizar o endereço que aparece no navegador, ignorando o padrão longo do .NET.


[Route("fale-conosco")]               // Deixa a rota mais limpa, sem o padrão /Home/Contato , deixando apenas /fale-conosco
public IActionResult Contato()                 // Nome da função que o .NET vincula ao arquivo Contato.cshtml
{
    return View();                             // Busca o arquivo na pasta Views/Home automaticamente
}


--> Categoria: Título e Interface (View - .cshtml)

Explicação: O ViewData serve para enviar informações da página específica para o Layout principal.


@{
    ViewData["Title"] = "Página Inicial de Estudos"; // Define o valor injetado na tag <title> do Layout (HTML)
}

<h1>Bem-vindo ao meu projeto de Rotas!</h1>      
<p>Escolha uma das páginas abaixo:</p>          

<ul>
    <li>
        <a href="/Home/Produtos">Ver Produtos</a> </li>
    <li>
        <a href="/fale-conosco">Ir para Contato</a> </li>
</ul>